<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE module PUBLIC
    "-//Checkstyle//DTD Checkstyle Configuration 1.3//EN"
    "https://checkstyle.org/dtds/configuration_1_3.dtd">

<!--
  Checkstyle configuration for the Tier Rating API.

  Checks essential code quality and naming conventions without enforcing
  strict formatting (formatting is handled by IDE settings / google-java-format).
-->
<module name="Checker">
    <property name="charset" value="UTF-8"/>
    <property name="severity" value="error"/>
    <property name="fileExtensions" value="java"/>

    <module name="TreeWalker">

        <!-- ===== Naming Conventions ===== -->
        <module name="ConstantName"/>
        <module name="LocalFinalVariableName"/>
        <module name="LocalVariableName"/>
        <module name="MemberName"/>
        <module name="MethodName"/>
        <module name="PackageName"/>
        <module name="ParameterName"/>
        <module name="StaticVariableName"/>
        <module name="TypeName"/>

        <!-- ===== Imports ===== -->
        <!-- Block internal JDK implementation imports -->
        <module name="IllegalImport"/>
        <!-- Block duplicate imports of the same class -->
        <module name="RedundantImport"/>

        <!-- ===== Miscellaneous ===== -->
        <!-- Enforce uppercase L for long literals (1000L not 1000l) -->
        <module name="UpperEll"/>
        <!-- Enforce Java-style array declarations (int[] arr, not int arr[]) -->
        <module name="ArrayTypeStyle"/>

    </module>
</module>
